

import 'package:flutter/material.dart';

class Espacamento extends StatelessWidget {
  const Espacamento({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
     height: 5.0,
    );
  }
}
